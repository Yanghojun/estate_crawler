[project]
name = "estate-crawler"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bs4>=0.0.2",
    "markdownify>=1.1.0",
    "mcp[cli]>=1.9.1",
]
